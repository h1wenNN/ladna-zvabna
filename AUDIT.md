# АУДИТ — сайт «Ладна-Звабна»

**Етап 8 · 28.08.2026 · режим 🔍 Аудитор**

Завдання цього етапу — не заспокоїти, а знайти проблеми у власній роботі.
По кожному пункту наведено доказ, а не оцінку.

---

## 1. Анти-ШІ чекліст

Автоматичну перевірку прогнано по `index.html`, чотирьох CSS і трьох JS.
**53 пункти, 3 спрацювання — усі три виявилися хибними.**

| Спрацювання | Вирок | Доказ |
|---|---|---|
| «фіолетово-синій градієнт» | ✅ хиба регулярки | Спіймала `#A89F8B` і `#E9DED0`. Прямий розбір **усіх** HEX у CSS на RGB: кольорів, де синій домінує над червоним і зеленим, — **0** |
| `translateY(40px)` | ✅ хиба регулярки | Збіг лише в **коментарі**, який цей прийом забороняє. У живому коді (з вирізаними коментарями) — 0. Реальний зсув `--shift: 14px` |
| «бороть» | ✅ задокументований виняток | «Не веде нескінченної **боротьби** зі своїм тілом» — пряма цитата клієнтки, і вона **заперечна** |

**Чисто по всіх 50 інших пунктах**, зокрема:

*Візуал:* немає `background-clip:text`, `backdrop-filter`, bento-сітки,
`border-radius:12px`, чорних тіней `rgba(0,0,0,…)`, кнопок-пігулок `9999px`,
емодзі в інтерфейсі, іконкових бібліотек, секції «Наша команда», стрічки
«Нам довіряють», каруселі відгуків, гарнітур Inter/Poppins/Montserrat/Roboto/Lato/Playfair.

*Поведінка:* немає `fade-up 40px`, підняття картки на hover, модалки підписки,
чат-бота, cookie-банера, кнопки «Наверх», `bounce`/`elastic` easing, кастомного
курсора, marquee, magnetic-кнопок, по-літерного розбиття, лічильників,
горизонтальної скрол-секції, скрол-джекінгу, 3D-tilt.

---

## 2. Вичитка текстів

Пошук по видимому тексту (без коментарів, скриптів і стилів):

| Слово | Знайдено |
|---|---|
| «ідеальн…» | 0 ✓ |
| «недолік» | 0 ✓ |
| «проблем…» | 0 ✓ |
| «виправ…» | 0 ✓ |
| «бороть…» | 1 — задокументований виняток вище |

Плюс жодного з: «Ласкаво просимо», «широкий спектр», «команда досвідчених
професіоналів», «Чому обирають нас», «Наші переваги», «індивідуальний підхід»,
«сучасне обладнання», «інноваційн…», «Трансформуйте», «Відкрийте для себе»,
«гарантован…», «знижк…», «акці…».

**Знаків оклику на сайті: 0.**

---

## 3. Типографіка

| | |
|---|---|
| Прямі лапки `"` `“` `”` | **0** ✓ |
| Прямі апострофи `'` | **0** ✓ |
| Ялинки «» | 4 / 4 (парні) ✓ |
| Довгі тире — | 30 ✓ |
| Дефіс у ролі тире (` - `) | **0** ✓ |
| Нерозривні пробіли | 186 |

---

## 4. Тести стійкості

### Без JavaScript
- **5458 символів тексту** — сторінка повна
- Прихованих блоків: **0**
- Прелоадер не показується
- Квіз працює нативно: 1 пункт відкрито, `name="care"` на місці → ексклюзивність зберігається
- До/після: **2 кадри поруч**, чесно й читабельно
- Відео: обидва показують **постер**, нічого не вантажать
- Горизонтального скролу немає

### `prefers-reduced-motion`
- Прихованих елементів: **0**
- Прелоадер: `display:none`
- Відео завантажено: **0**
- Активних CSS-анімацій: **0**

### Повільна мережа (3G: 400 кбіт/с, RTT 400 мс)
- DOM готовий: **2503 мс**
- Шрифти: 4357 мс
- Текст на місці: 5516 символів, герой видно **одразу**

### Тільки клавіатура
Пройдено 60 натискань Tab. **Без видимого фокуса — жодного елемента.**
Порядок обходу (18 цілей до замикання циклу):

```
 1 Перейти до змісту        10 Хочу відчути тіло легшим
 2 логотип                  11 Хочу глибоке очищення
 3 Історія                  12 Хочу підтримати пружність
 4 Простір                  13 Ще не знаю — підкажіть
 5 Догляди                  14 повзунок до/після
 6 Ціни                     15 Написати в Instagram
 7 Написати                 16 +380 75 686 09 12
 8 Про наш простір ↓        17 Бровари, вул. Симоненка, 30А
 9 Хочу, щоб шкіра відпочила 18 @ladna_zvabna
```

---

## 5. Чотири ширини

| Ширина | Висота сторінки | Горизонтальний скрол | Обрізаний текст |
|---|---|---|---|
| 375 px | 17 149 px | немає ✓ | нічого ✓ |
| 768 px | 17 287 px | немає ✓ | нічого ✓ |
| 1440 px | 16 353 px | немає ✓ | нічого ✓ |
| 1920 px | 16 740 px | немає ✓ | нічого ✓ |

Перша перевірка спіймала два `.sr-only` заголовки — це навмисна техніка для
скрінрідерів (стиснуті до 1 px), а не дефект. Після виключення — чисто.

---

## 6. Дев'ять критеріїв приймання

| # | Критерій | Стан |
|---|---|---|
| 1 | Людина за 30 с може описати відчуття, а не перелік послуг | 🟡 **суб'єктивно, перевірити на живих людях** |
| 2 | Історію неможливо проскролити не зупинившись | 🟡 **суб'єктивно** |
| 3 | Жодного пункту анти-ШІ чекліста | ✅ 53 пункти перевірено |
| 4 | Красивий і читабельний без JS і при reduced-motion | ✅ доведено вище |
| 5 | Lighthouse ≥ 90 / 95 / 100 / 100 | ✅ **97 / 100 / 100 / 100** |
| 6 | Немає слів «ідеальна», «недолік», «проблема», «боротьба», «виправити» | ✅ з одним задокументованим винятком |
| 7 | На 375 px без горизонтального скролу й обрізаного тексту | ✅ |
| 8 | Заміна фото = заміна файлів, без правок коду | ✅ |
| 9 | Клієнтка каже «це наш салон» | ⬜ **не можу перевірити — це до Ірини** |

**6 з 9 доведено. 2 суб'єктивні. 1 поза моєю досяжністю.**

---

## 7. ЩО НЕ ЗРОБЛЕНО

Розділ навмисно окремий, бо його найлегше не помітити.

### Блокує публікацію

1. **Графік роботи.** У розділі «Запис» стоїть видимий напис
   `[УТОЧНИТИ: дні та години роботи]`. Я не став вигадувати.
   Через це в JSON-LD також немає `openingHours` — пошук показує ці години
   людям, і краще не вказати нічого, ніж вигадане.
2. **Домен.** У `index.html`, `sitemap.xml` і `robots.txt` — плейсхолдер
   `ladna-zvabna.com.ua`, **6 згадок**. Замінити перед заливкою.

### Варто зробити, але не блокує

3. **Ціни** взято зі старого сайту й не підтверджено.
4. **Реального фотосету немає.** Дев'ять потрібних кадрів описано в `PROGRESS.md`
   з іменами `data-shot`. До/після — плейсхолдери; повзунок побудований і працює,
   але порівнювати поки нічого.
5. **Мікророзмиття 1.6 px на фоні героя** — милиця для кадру 720 px,
   розтягнутого на всю ширину. **Прибрати з появою фотосету**
   (`components.css`, розділ 5.1).
6. **Шрифтова пара** — резервна (Cormorant Infant + Onest). Пріоритетну
   Kyiv Type Serif дістати не вдалося.
7. **Крос-браузер перевірено лише в Chromium.** Firefox і WebKit у цьому
   середовищі не встановилися. Особливо варто відкрити на **Safari iOS**:
   там інакше поводяться `overflow:clip`, `position:sticky` і `100vh`.

### Свідомо не робилося

8. Секції «Наша команда» немає — імена майстрів не підтверджено.
9. Відгуків немає — вигадувати їх заборонено правилом B2.
10. Cookie-банера немає, бо немає аналітики. З'явиться аналітика — знадобиться банер.

---

## 7-біс. Знайдено вже після аудиту

При перевірці «як людина відкриє це в себе» виявилося: **при відкритті
подвійним кліком (`file://`) логотип у шапці, футері та прелоадері зникав** —
браузер блокує зовнішній SVG як CSS-маску з файлового протоколу.

Виправлено: два знаки, які використовує сторінка, вбудовано в CSS
як `data:URI`. Тепер сайт коректний і через сервер, і подвійним кліком.
Ціна — `site.min.css` виріс із 7.5 до 13.2 КБ у gzip, Lighthouse
Performance 98 → **97**. Обмін виправданий: логотип має бути видно завжди.

Додано `start.bat` — запуск локального сервера одним кліком.

---

## 8. Підсумок

Сайт технічно готовий. Дві речі тримають його від публікації, і обидві
потребують не коду, а однієї відповіді від Ірини: **графік роботи** і **домен**.

Найцінніше, що дав цей етап, — підтвердження, що три «знахідки» автоматичної
перевірки виявилися хибними спрацюваннями. Якби я зупинився на першому прогоні,
у звіті стояло&nbsp;б «знайдено 3 порушення» — і це була&nbsp;б неправда.

---

## 9. СКОРОЧЕННЯ САЙТУ · 29.08.2026

Зауваження замовника: «перший екран має більше чіпляти; сайт має бути
компактнішим, без нескінченного скролу».

### 9.1 Що дало довжину

Заміри висот секцій на 1440×900 **до** роботи — сторінка 16 494 px, це
18,3 екрана. Три чверті зайвого припадали на чотири місця:

| Секція | Було | Стало | Чим це було |
|---|---|---|---|
| Догляди + Ціни | 2 240 + 1 461 | **1 539** | дві секції з ОДНИМ І ТИМ САМИМ переліком із 12 процедур |
| Глава «Друге народження» | 2 715 | 1 577 | `min-height:140vh` під один прилиплий заголовок і 160 px між кожними двома реченнями |
| Простір | 1 753 | 1 234 | розсип деталей у два ряди |
| Консультація | 1 370 | 822 | заголовок над квізом, хоча поруч було порожньо |

### 9.2 Головне рішення — злиття доглядів і цін

Це не «стиснення відступів», а виправлення справжньої вади: читач двічі
проходив ті самі дванадцять назв — спершу з описами й тривалістю, потім
із цінами. Тепер один рядок несе все: назва · ціна · тривалість · опис.
Дві групи стоять поруч. Секція цін видалена разом зі своїм CSS
(`.price-list`, `.price-row*`, `.price__*` — 46 рядків), у навігації
замість двох пунктів один — «Догляди й ціни». Якір `#price` збережено:
він тепер на списку, тож старі посилання не ламаються.

### 9.3 Ритм

`--sp-section` 18vh → 10vh, `--sp-breath` 9vh → 6.5vh. Це не «зробити
все дрібнішим»: рваний ритм між секціями лишився, змінився масштаб паузи.
Проза й заголовки не зменшені **ніде**.

### 9.4 Кадри

Розсип у секції «Простір» був із двох рядів по чотири кадри, серед них
вікно (те саме зауваження замовниці) і шафа з коробочками. Тепер один ряд
із трьох: тканина крісла й тераццо, картина в позолоченій рамі, різьблення
комода. Два кадри перекадровано під це:

- `d-carved` — зрізано верх із упаковками, лишилося саме різьблення (640×400);
- `d-terrazzo` — зрізано журнальницю праворуч (560×560);
- `d-portrait` — новий кадр із постера відео `portrait`, яке ніде не грало.

`d-candles` прибрано з розсипу: це майже той самий кадр, що постер відео
в секції запису. Повтор — саме те, на що скаржилася замовниця.

### 9.5 Знайдені при цьому баги

| Баг | Суть |
|---|---|
| **Кадр героя на мобільному завширшки 1/12 екрана** | `.hero__frame` у мобільному блоці не мав `grid-column`, тож автоплейсмент клав його в одну колонку з дванадцяти. Видно було вертикальну смужку фото |
| **Пара «до/після» 307 px замість 520** | `max-width` + `margin-inline:auto` у грід-елемента дає ширину `fit-content`, а не обмежений stretch. Треба `width` |
| **Підпис адреси на кадрі — контраст не гарантований** | `text-shadow` не є тлом. Додано градієнтне затемнення в нижній частині кадру; вимір реальних пікселів — 5,47:1 |
| **`·` між фактами переносилася на окремий рядок** | роздільник — абсолютно позиційований `::before` сусіда; при переносі лишався сам. Нижче 560 px список став стовпчиком |

### 9.6 Результат

| | до | після |
|---|---|---|
| Висота сторінки (1440) | 16 494 px | **11 105 px** |
| Екранів прокрутки | 18,3 | **12,3** |
| Lighthouse Performance (desktop / mobile) | 95 / — | **100 / 94** |
| Accessibility / Best Practices / SEO | 100 | **100** |
| CLS / TBT | 0,01 / 0 | **0,01 / 0 мс** |
| Контраст усіх текстів | 0 порушень | **0 порушень** |
| 360…1920 px | без скролу | **без скролу** |
| Без JS · reduced-motion · 3G · клавіатура | проходить | **проходить** |

Тексту на сторінці — 5 768 знаків: жодного речення не викинуто.
Коротше стало за рахунок дублювання й порожнечі, не за рахунок змісту.

---

## 10. ПОЛЮВАННЯ НА ПОМИЛКИ · 30.08.2026

Завдання: знайти накладання елементів і будь-які інші помилки й виправити
все. Замість того, щоб дивитися на сторінку очима, написано **десять
перевірок**, які дивляться за мене — і повторюються після кожної правки
(`bash checkall.sh`). Нижче — що вони знайшли.

### 10.1 Найгірша знахідка: блок міг лишитися невидимим НАЗАВЖДИ

`IntersectionObserver` повідомляє лише про **перетин порогів**. Якщо блок
за один кадр стрибнув з-під нижнього краю екрана вище за верхній —
швидкий скрол, перехід за якорем, відновлення позиції браузером,
Ctrl+End — перетину не сталося, колбек не спрацював, і блок лишався
з `opacity:0` **до перезавантаження сторінки**.

На 768px це відтворювалося у двох випадках із трьох: обидві кнопки героя
(«Про наш простір», «Догляди й ціни») просто не з'являлися.

Виправлено трьома шарами:

1. **Підмітання після зупинки скролу.** Усе, що лишилося невидимим вище
   за екран, показується миттєво без анімації — анімувати те, що людина
   вже проминула, безглуздо. Усе, що висить видимим понад півсекунди і
   досі не проявилося, показується з анімацією.
2. **Спостерігаємо батька, а не обрізаний елемент.** `clip-path:inset(80%)`
   лишає від арки нижню п'яту частину, і саме її враховує спостерігач.
   Для арки, вищої за пів екрана, ця смуга заходить у видиму зону лише
   тоді, коли верх кадру вже проїхав угору. Тепер спостерігаємо
   необрізаний `<figure>`.
3. **Читання окремо від запису.** Перша версія підмітання міряла й одразу
   вішала клас у тому самому циклі — кожен наступний вимір змушував
   браузер перерахувати розкладку. TBT підскочив з 10 до 280мс. Після
   поділу на дві фази — знову норма.

Перевірка `revealcheck` проганяє шість різких сценаріїв на трьох ширинах:
**невидимих блоків 0**.

### 10.2 Мобільне меню не було чим закрити

Полотно меню (`z-index:70`) лежить **усередині** `<header>`. Кнопка
«Закрити» — теж у шапці. Нащадок із додатним z-index завжди малюється
над вмістом свого предка, тому кнопка опинялася під суцільним молочним
полотном. Закрити меню можна було тільки клавішею Esc — якої на телефоні
немає.

Підняти z-index самої шапки не допомагає (полотно в її ж стековому
контексті). Піднято внутрішній ряд `.hdr__in` — сусіда полотна.

Там само знайшлося ще два дефекти:

| Дефект | Суть |
|---|---|
| **Фокус-трап протікав** | Кнопка додавалася в кінець списку, а в DOM вона стоїть ПЕРЕД полотном. «Останній» елемент списку не збігався з останнім за Tab, замикання не спрацьовувало — і Tab з останнього пункту йшов у сторінку під меню |
| **Швидке закрити→відкрити гасило меню** | Відкладене `menu.hidden = true` з попереднього закриття спрацьовувало вже після нового відкриття |
| **Меню не влазило в екран** | 375×667 — 686px вмісту; у горизонталі телефона — 782px на 360px екрана. Відступи й кегль прив'язано до висоти вікна |

### 10.3 Акордеон лишав по 32px порожнечі на кожному пункті

`height:0` при `box-sizing:border-box` не може бути меншим за падінги, а
в тіла пункту був нижній падінг 32px. П'ять закритих пунктів = 128px
мертвого місця, і смужка невидимого тексту накривала сусідній заголовок.
Тепер падінг анімується разом із висотою, а `:not([open])` дає гарантований
нуль.

Друга помилка там само: швидкі кліки лишали відкритим **не той** пункт,
по якому клікнули — таймери попередніх закриттів спрацьовували пізніше.
Стан веде `transitionend` і явний `data-state`; таймер лишився тільки
як страховка.

### 10.4 Кожен якір вів під шапку

Шапка липка й заввишки 77–90px. Перехід за будь-яким посиланням меню
ставив секцію під неї: у прайсі під шапкою зникав заголовок першої групи.
Додано `scroll-margin-top` і токен `--hdr-h` (він же прибрав «магічні
90px» з розрахунку висоти героя).

### 10.5 Розтушовані краї темних сцен накривали текст

Смуги на межах сцени «Друге народження» і притчі — це **білі градієнти
поверх вмісту** (`z-index:3`). Їхня висота була фіксована (110–200px),
а поле секції я перед тим зменшив до 10vh. На низькому екрані
(1024×500, телефон у горизонталі) смуга виявлялася вищою за поле й
з'їдала перші та останні рядки. Тепер висота смуги дорівнює полю секції.

### 10.6 Дрібніше

| Що | Було | Стало |
|---|---|---|
| Кадр героя на мобільному | без `grid-column` у мобільному блоці — автоплейсмент клав його в одну колонку з дванадцяти, від фото лишалася вертикальна смужка | на всю ширину, перед текстом |
| Пара «до/після» | `max-width` + `margin-inline:auto` у грід-елемента дає ширину `fit-content` → 307px замість 520 | `width`, 520px |
| Підпис адреси на кадрі | `text-shadow` не є тлом; гарантії контрасту не було | градієнтне затемнення, вимір по пікселях — 4,97:1 |
| Підпис плейсхолдера | `rgba(ink,.42)` — 1,1:1, тобто людина бачила порожню фігуру й не розуміла, що там буде кадр | 8,03:1, текст «фото після зйомки» |
| Зони дотику | текстові лінки 26–30px заввишки | невидимий `::before` розширює зону до 44px, вигляд не змінився |
| Роздільник «·» між фактами | переносився на окремий рядок сам, без слова | нижче 560px список стає стовпчиком |
| `[УТОЧНИТИ: дні та години роботи]` | технічна помітка на живій сторінці | «за попереднім записом» + коментар у коді |
| Розміри відео banquette | заявлено 720×600, файл 720×450 | виправлено |
| Висячі слова | «шия» лишалася сама на рядку на 320 і 375px | нерозривні пробіли |
| Скрипти перевірок | 49 файлів лежали в корені сайту й поїхали б на хостинг | `_dev/checks` і `_dev/scratch` |

### 10.7 Друк

Стилів друку не було зовсім. Прайс салону роздруковують, тож тепер
на папір іде документ: темні сцени світлі, декор і кадри прибрані
(вони підвантажуються ліниво — людина, яка натиснула «Друк» не догортавши
сторінку, отримала б аркуші з порожніми рамками), перелік доглядів і цін
починається з чистого аркуша, контакти зібрані в таблицю.

Три помилки, знайдені саме на друку і **важливі не лише для нього**:

- `overflow:clip` на секціях обрізав текст, який мав перейти на наступний
  аркуш;
- сітка не розривається між аркушами — `break-inside:avoid` у грід-елементах
  Chromium ігнорує, тому на друку розкладка стає звичайним потоком;
- `opacity:1 !important` у скиданні зробив видимими білі градієнтні смуги
  темних сцен — і вони накрили цитату з притчі. Саме це й привело до
  знахідки 10.5.

### 10.8 Чим перевіряти далі

`bash checkall.sh` — десять скриптів, повний прогін близько 12 хвилин:

| Скрипт | Що ловить |
|---|---|
| `layoutcheck` | накладання, обрізаний текст, вихід за вікно, зони дотику — 16 ширин від 320 до 2560 |
| `statecheck` | те саме у станах: меню відкрите, квіз розкритий, повзунок у крайніх положеннях, шрифт +30%, короткі екрани |
| `revealcheck` | чи не лишається щось невидимим після різких переходів |
| `qualitycheck` | розмітка, посилання, доступність, фокус, без JS, reduced-motion |
| `menucheck` | меню: закриття, Esc, фокус-трап, перехід за пунктом |
| `mediacheck` | відео, повзунок «до/після», вхід за адресою з якорем |
| `hitcheck` | реальні зони натискання пальцем |
| `bandcheck` | смуги на краях темних сцен не накривають текст |
| `typocheck` | висячі короткі слова в кінці рядків |
| `fullcheck` | контраст кожного тексту, зокрема поверх кадрів — по справжніх пікселях |

**Підсумок прогону: усі десять — 0 проблем.**
Lighthouse: desktop **100 / 100 / 100 / 100**, mobile **93 / 100 / 100 / 100**.

---

## 11. АДАПТИВНІСТЬ · 31.08.2026

Зауваження замовника: «перший екран цілком не поміщається на екран,
тому виглядає трохи негарно; підкоректуй увесь сайт під різні
розширення екранів, мобільні також».

Перевірку переведено з «круглих ширин при фіксованій висоті 900px» на
**реальні пристрої**: 23 конфігурації від iPhone SE (320×568) до 4K,
включно з горизонтальною орієнтацією й низькими вікнами ноутбуків.
Висота вікна впливає на розкладку не менше за ширину — саме цього
попередня перевірка не бачила зовсім.

### 11.1 Перший екран не вміщався на 9 конфігураціях із 30

Заміри ДО: герой перевищував екран на телефонах (320×568, 360×640,
375×667, 390×844, 360×780), на планшеті в горизонталі (1024×768),
на телефоні в горизонталі (740×360, 844×390, 932×430) і на ноутбуках
1280×720 та 1366×768. На 375×667 герой мав 830px замість 590 —
кнопки лишалися на 240px нижче за згин.

Причин було дві.

**Розміри залежали лише від ширини вікна.** `7vw` на ноутбуці 1280×720
дає заголовок 90px; три рядки такого заголовка — 243px, і кнопкам уже
немає де стати. Тепер кожен розмір у героєві має ДРУГИЙ обмежувач за
висотою вікна і нижню межу, щоб на низькому вікні текст не зменшився
до нечитабельного:

| | було | стало |
|---|---|---|
| заголовок | `clamp(3rem, 7vw, 8rem)` | `min(clamp(3rem, 7vw, 8rem), 15vh)` |
| лід | `var(--fs-lead)` | `clamp(15px, 2.9vh, 1.15rem)` |
| поля секції | `clamp(32px, 5vh, 72px)` | `clamp(16px, 3.2vh, 64px)` |
| відбивки заголовка | `--sp-6 / --sp-8` | `clamp(8px,1.8vh,24px) / clamp(12px,2.8vh,32px)` |

**Розкладку вибирала ширина, а не орієнтація.** Телефон 320×568 —
вертикальний екран, але за старим правилом «≤1024px і низьке вікно»
він діставав розкладку для телефона в горизонталі. Тепер розкладок три
й вибирає між ними орієнтація:

- широкий екран нормальної висоти — текст ліворуч, кадр праворуч;
- **вертикальний екран** — кадр згори, текст під ним;
- **низьке горизонтальне вікно** — компактно, без вимоги повного екрана
  (360px висоти не вміщають ані кадру, ані заголовка в три рядки —
  вимагати там 100svh безглуздо).

**Кадр забирає рівно те, що лишилося.** У вертикальній розкладці
`.hero__in` дістав ФІКСОВАНУ висоту, а не `min-height`: у розтяжного
контейнера кадр брав свій максимум (330px) і виштовхував текст за екран.
Тепер текст нерозтяжний, кадр — розтяжний, і екран заповнений рівно.

**Результат:** усі 30 конфігурацій — перший екран уміщається,
кнопки видно.

### 11.2 Підпис адреси на мобільному був НЕВИДИМИЙ

`<figcaption>` лежить усередині `<figure class="hero__frame">`, а в тієї
`overflow:hidden`. На широкому екрані підпис абсолютний і лежить у кутку
кадру — усе гаразд. На вузькому він ставав звичайним рядком під кадром —
тобто за межами `overflow:hidden`, і його просто не було видно.

Тепер обрізає сама арка, а `<figure>` стала колонкою: кадр і підпис —
два її елементи.

### 11.3 Планшет у горизонталі втрачав половину ширини

Смуги сітки розпадалися на стос при `max-width:1024px`, тож iPad 1024×768
діставав мобільну розкладку: квадратна арка на всю ширину — це кадр
1024×1024, і секція «Запис» виростала до 1556px замість 691.

- поріг смуг перенесено на **900px**;
- медіа-блоки на вузьких екранах дістали стелю `max-width:440px`
  (`justify-self`, а не `margin:auto` — у грід-елемента автополя
  перетворюють ширину на `fit-content`);
- розсип деталей тримає три кадри в ряд до 520px, а не до 768.

**Сторінка на iPad у горизонталі: 11 643 → 9 769px.**

### 11.4 Кадр героя не доходив до краю на великих екранах

`.hero__in` мала `position:relative`, тож абсолютний кадр чіплявся до
неї — а це `.wrap` із `max-width:1440px`. На екрані 2560px кадр висів
прямокутником посеред порожнечі за 560px від краю. Прибрано
`position:relative`; стеля ширини кадру піднята з 720 до 860px —
розтягнути м'яке фото на 1,2 не страшно, а лишити 720px на 2560px це
вже марка, а не кадр.

### 11.5 Висота шапки була приблизною

`--hdr-h` використовують і герой (`100svh` мінус шапка), і відступ для
якорів, але сама шапка виводила висоту з вмісту — тобто токен був
здогадкою. Тепер `.hdr__in` має `height:var(--hdr-h)`, і на всіх
23 конфігураціях виміряна висота шапки збігається з токеном точно.

### 11.6 Дрібніше

| Що | Було | Стало |
|---|---|---|
| Роздільник «·» між фактами | абсолютна крапка між пунктами — при переносі лишалася сама на рядку (і на телефоні, і у вузькій колонці планшета) | `::after` усередині пункту: фізично належить своєму слову |
| Довге слово при збільшеному шрифті | «Аквадермагенез-гідрошліфовка» на 320px зі шрифтом +30% розсувала секцію на 31px | `overflow-wrap:break-word` глобально, `anywhere` на назвах |
| Колонка доглядів | `1fr` не звужується менше за `min-content` — група виходила за сітку на 37px | `min-width:0` |
| Ціна на найвужчих екранах | «1200 · 1800 грн» не лишала назві місця | нижче 340px ціна стає окремим рядком |
| Підписи «до/після» | виходили за екран на 3px при зумі 200% | `min-width:0` + перенос |
| Адреса в героєві на 320px | два рядки, з'їдала висоту кадру | щільніший трекінг — один рядок |
| Дві дії героя на 320px | переносилися одна під одну, −50px висоти кадру | менший кегль нижче 390px, один рядок |
| Пауза й прилипання історії | `42vh` і `64vh` — на 4K це 907 і 1382px порожнечі | стелі `min(42vh,380px)` і `min(64vh,620px)` |

### 11.7 Що перевірено

| | результат |
|---|---|
| Перший екран уміщається | 30 конфігурацій із 30 |
| Накладання, обрізаний текст, вихід за вікно | 23 пристрої — 0 |
| Стани (меню, квіз, повзунок, шрифт +30%) | 10 вікон — 0 |
| Зум тексту 100 / 130 / 150 / 200% на 320, 360, 375 | 0 |
| Без JS на 320…1440 | герой уміщається, прихованих 0, скролу вбік немає |
| Lighthouse desktop | 100 / 100 / 100 / 100 |
| Lighthouse mobile | 93 / 100 / 100 / 100 |

**Довжина сторінки після адаптації**

| | телефон 375 | iPad порт. | iPad ланд. | ноут 720 | десктоп | FullHD | 4K |
|---|---|---|---|---|---|---|---|
| px | 12 283 | 12 863 | 9 750 | 9 978 | 11 105 | 12 009 | 13 992 |
| екранів | 18,4 | 12,6 | 12,7 | 13,9 | 12,3 | 11,1 | 6,5 |

### 11.8 Лишилося відомим

CLS 0,029 (норма — до 0,1) дає одноразове перешикування заголовка
героя, поки вантажиться Cormorant: запасна Georgia на цьому кеглі
переносить рядок інакше. Прибрати повністю можна лише `font-display:
optional` — тобто ціною того, що на повільному першому візиті людина
взагалі не побачить фірмової гарнітури. Шрифт бренду важливіший.
